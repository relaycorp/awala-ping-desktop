describe('runMaintenance', () => {
  describe('Expiring certificates', () => {
    test.todo('Certificates with fewer than 90 days left should be renewed');

    test.todo('Certificates with more than 90 days left should not be renewed');
  });
});
