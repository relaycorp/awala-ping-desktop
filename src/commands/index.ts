import * as ping from './ping';
import * as register from './register';
import * as thirdPartyEndpoints from './thirdPartyEndpoints';

export const commands: readonly any[] = [ping, register, thirdPartyEndpoints];
